/* 2022-11-26T03:31:50.545Z */
(()=>{"use strict";feather.replace()})();const filterOptions=(needle,optionItems)=>{var needle=needle.replace(/[-\/\\^$*+?.()|[\]{}]/g,"\\$&"),pat=new RegExp(needle,"i");for(let i=0;i<optionItems.length;i++){var item=optionItems[i];pat.test(item.innerText)?item.classList.remove("d-none"):item.classList.add("d-none")}},sleep=m=>new Promise(r=>setTimeout(r,m)),clearSelectOptions=selectBox=>{for(let i=selectBox.options.length-1;0<=i;i--)selectBox.remove(i)},populateSelectOptions=(selectBox,newItems,except=[])=>{newItems.forEach(function(item){let skip=!1;0<except.length&&(skip=-1<except.findIndex(exceptItem=>exceptItem.agentId==item.agentId));skip||(selectBox.options[selectBox.options.length]=new Option(item.agentName,item.agentId))})};